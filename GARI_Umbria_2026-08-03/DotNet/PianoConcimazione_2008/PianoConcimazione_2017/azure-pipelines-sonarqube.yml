name: $(Date:yyyyMMdd).$(Rev:r)

trigger: none

schedules:
  - cron: '30 6 * * *' # cron syntax defining a schedule (07:30)
    displayName: nightly
    branches:
      include: [ develop ] # which branches the schedule applies to

parameters:
  - name: executeDeploy
    displayName: Flag controllo deploy
    type: boolean
    default: false

variables:
- group: Destinations
- template: ../../templates/variables/deploy-path.yml
  parameters:
    branchName: ${{ replace(variables['Build.SourceBranch'], 'refs/heads/', '') }}

extends:
  template: ../../templates/pipelines/net-agro-web-pipeline.yml  # Template reference
  parameters:
    solution: PianoConcimazione_2008
    target: PianoConcimazione_2017
    artifactName: 'AgronicaPianoConcimazione_2017'
    path: 'PianoConcimazione_2008'
    artifactBasePath: PianoConcimazione_2017
    outdir: 'PianoConcimazione_2017'
    sonarExcludeDuplicates: true
    execDeploy: ${{ parameters.executeDeploy }}
    deployPath: $(PathDeployFinal)
    preBuildSteps:
      - template: ../../templates/steps/sonar-pre-build.yml
        parameters:
          scannerMode: 'dotnet'
          projectKey: 'GiasDotNet-PianoConcimazione'
          projectName: 'GiasDotNet - Piano Concimazione'
          projectVersion: '$(SonarVersion)'
          #verbose: 'true'
    postBuildSteps:
      - template: ../../templates/steps/sonar-post-build.yml
