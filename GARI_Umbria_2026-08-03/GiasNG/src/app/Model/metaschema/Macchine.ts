import {BaseCodeDescrStr} from "../baseClass/baseCodeDescrStr";

export class Macchine extends BaseCodeDescrStr {
  constructor(codice: string = '', descrizione: string = '') {
    super(codice, descrizione);
  }
}
